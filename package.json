{
  "name": "@independo/inderun-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "generate": "node contracts/scripts/generate-contracts.mjs",
    "upgrade": "corepack prepare pnpm@latest --activate && pnpm --recursive update"
  },
  "devDependencies": {
    "quicktype": "^23.2.6",
    "typescript": "^5.9.3"
  }
}
