{
  "name": "@independo/inderun-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "generate": "pnpm -r generate"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
