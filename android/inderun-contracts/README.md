# IndeRun Contracts (Android)

This module contains generated Kotlin contract models for the schema-backed IndeRun payloads used by the Android SDK.

The source of truth remains `contracts/schemas/*.schema.json`. Regenerate this module with:

```sh
pnpm generate
```
