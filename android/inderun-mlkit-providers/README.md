# IndeRun ML Kit Providers (Android)

This module contains Android provider adapters backed by ML Kit GenAI APIs.

For Issue 9, it provides the on-device `AndroidMlKitGenAiProvider` implementation for Mode-1 `text_to_text` requests.
